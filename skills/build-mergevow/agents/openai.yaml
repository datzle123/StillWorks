interface:
  display_name: "Build MergeVow"
  short_description: "Build MergeVow without moving the goalposts"
  default_prompt: "Use $build-mergevow to implement the next approved MergeVow issue within its trust boundaries."
